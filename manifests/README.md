# Manifests

This directory contains reviewed source manifests and other reproducibility
inputs.

Current files:

- `source-manifest.audit.jsonl`: first real source-repository candidates
  approved for audit-only extraction.

Manifest entries are not training approval. They only say that a repository
revision passed the first source-license screen. Teacher labels, dataset
release, and model training each require their own recorded approval.
